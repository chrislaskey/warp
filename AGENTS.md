This repository explores a pattern for rapidly iterating on remote Elixir apps running in Docker.

The `example` directory is a new Phoenix LiveView application originally generated with:

- `mix phx.new example --database sqlite3`
- `flyctl launch`
